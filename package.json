{
	"name": "run-install-script",
	"version": "1.0.1",
	"scripts": {
		"install": "concurrently \"cd SERVER && npm install\" \"cd UI && npm install\"",
		"dev": "concurrently \"cd SERVER && npm run dev\" \"cd UI && npm run dev\"",
		"test": "concurrently \"cd SERVER && npm run test\" \"cd SERVER && npm run test\"",
		"test:server": " cd SERVER && npm run test ",
		"test:ui": "cd UI && npm run test"
	},
	"dependencies": {
		"concurrently": "^8.2.2"
	}
}
